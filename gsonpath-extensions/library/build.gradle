apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'signing'

group = project_group
version = extensions_version

targetCompatibility = JavaVersion.VERSION_1_7
sourceCompatibility = JavaVersion.VERSION_1_7

dependencies {
    compile project(":gsonpath-core:gsonpath-core-library")
    compile "com.google.code.gson:gson:$gson_version"
}

buildscript {
    repositories {
        mavenCentral()
    }
}

task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from 'build/docs/javadoc'
}

task sourcesJar(type: Jar) {
    from sourceSets.main.allSource
    classifier = 'sources'
}

artifacts {
    archives jar
    archives javadocJar
    archives sourcesJar
}

if (project.hasProperty('sonatypeUsername') && project.hasProperty('sonatypePassword')) {

    signing {
        sign configurations.archives
    }

    uploadArchives {
        repositories {
            mavenDeployer {
                beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }

                repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
                    authentication(userName: sonatypeUsername, password: sonatypePassword)
                }

                pom.artifactId = 'gsonpath-extensions'

                pom.project {
                    name 'gsonpath-extensions'
                    packaging 'jar'
                    description 'A Gson Path extension library that enhances the existing validation'
                    url 'http://lachlanmckee.net/gsonpath/'

                    scm {
                        url 'scm:git@github.com:LachlanMcKee/gsonpath.git'
                        connection 'scm:git@github.com:LachlanMcKee/gsonpath.git'
                        developerConnection 'scm:git@github.com:LachlanMcKee/gsonpath.git'
                    }

                    licenses {
                        license {
                            name 'MIT License'
                            url 'http://www.opensource.org/licenses/mit-license.php'
                            distribution 'repo'
                        }
                    }

                    developers {
                        developer {
                            id 'lachlanmckee'
                            name 'Lachlan McKee'
                        }
                    }
                }
            }
        }
    }
}